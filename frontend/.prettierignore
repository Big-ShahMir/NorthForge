dist
coverage
node_modules
package-lock.json

# Generated by `npm run generate:api` (see backend/northforge/api/export_openapi.py).
# Formatting these would fight the generator; CI's `contracts` job regenerates
# and diffs them instead of checking their style.
openapi.json
src/lib/api-schema.d.ts
